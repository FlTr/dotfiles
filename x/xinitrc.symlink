#!/bin/sh
#
# ~/.xinitrc

# virtualbox
if [ -f /usr/bin/VBoxClient-all ] ; then
    sudo /usr/bin/VBoxClient-all
fi

if [ -d /etc/X11/xinit/xinitrc.d ]; then
  for f in /etc/X11/xinit/xinitrc.d/*; do
    [ -x "$f" ] && . "$f"
  done
  unset f
fi

~/.dotfiles/bin/remap-kb

exec i3
